# Sanity project the web preview reads from. Required.
SANITY_PROJECT_ID=your-project-id
SANITY_DATASET=production

# Optional — only needed for private datasets. Proxied server-side; never
# exposed to the browser.
# SANITY_TOKEN=sk...
